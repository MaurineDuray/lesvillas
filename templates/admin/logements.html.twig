{% extends 'base.html.twig' %}

{% block title %}Villas Blue{% endblock %}

{% block body %}
    <div class="slide" id="introGalery">
         <img id="intro-logo" src="{{asset('images/logo-03.png')}}" alt="Logo">
        <div class="sep">
            <img src="{{asset('images/separation-bottom.png')}}" alt="separation">
        </div>
    </div>
    <div class="slide" id="dasboardTitle">
        <h3 class="script"> Boujour {{app.user.firstName}},</h3>
        <h2>Bienvenue dans le tableau de bord !</h2>
        <a href="{{path('admin')}}">Retour au tableau de bord</a>
        <div class="addBtn">
            <a href="{{path('admin_logement_add')}}" id="addButtons"><button>Ajouter un logement au catalogue</button></a>
        </div>
    </div>
   <div class="tableau">
   <h1 style="text-align: center">Gestion des logements du catalogue</h1>
    <table>
        <thead>
            <tr>
                <th>ID</th>
                <th>Logement</th>
                <th>Conciergerie</th>
                <th>Galerie</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            {% for immo in immos %}
                <tr>
                    <td>{{immo.id}}</td>
                    <td><a href="{{path('logement',{'id':immo.id})}}">{{immo.Titre}}</a></td>
                    <td>{{immo.conciergerie}}</td>
                    <td><a href="{{path('admin_galery',{'id': immo.id})}}">Gérer la galerie</a></td>
                    <td class="actions">
                        <a href="{{path('admin_logement_edit',{'id': immo.id})}}" class="btn-yellow"><i class="fas fa-edit"></i></a>
                        <a href="{{path('admin_logement_img', {'id':immo.id})}}"><i class="fa-solid fa-image"></i></a>
                        <a href="{{path('admin_logements_delete',{'id': immo.id})}}" class="btn-red" ><i class="fas fa-trash"></i></a>
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
   </div>
    </div>
    {% include "partials/footer.html.twig" %}
{% endblock %}
