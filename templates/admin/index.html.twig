{% extends 'base.html.twig' %}

{% block title %}Villas Blue{% endblock %}

{% block body %}
    <div class="slide" id="introGalery">
         <img id="intro-logo" src="{{asset('images/logo-03.png')}}" alt="Logo">
        <div class="sep">
            <img src="{{asset('images/separation-bottom.png')}}" alt="separation">
        </div>
    </div>
    <div class="slide" id="dasboardTitle">
        <h2>Bienvenue dans le tableau de bord !</h2>
        <a href="{{path('account_logout')}}">Déconnexion</a>
    </div>
    <div class="slide" id="containerAdminButton">
        <div class="buttons" style="display: flex; justify-content:center">
           <a href="{{path('admin_logement')}}"><button class="azurButton">Gestion des logements</button></a>
            <a href="{{path('admin_activities')}}"><button class="florideButton">Gestion des activités</button></a>
            <a href="{{path('admin_faq')}}"> <button class="azurButton">Gestion de la FAQ</button></a>
            <a href="{{path('admin_partners')}}"> <button class="florideButton">Gestion des partenaires</button></a>
            <a href="{{path('admin_user')}}"> <button class="florideButton">Gestion des administrateurs</button></a>
            <a href="{{path('admin_contacts')}}"> <button class="azurButton">Gestion des contacts</button></a>
            <a href="{{path('admin_reservations')}}"> <button class="florideButton">Gestion des réservations</button></a>
            <a href="{{path('admin_conciergerie')}}"> <button class="azurButton">Gestion de la conciergerie</button></a>
            <a href="{{path('admin_blog')}}"> <button class="azurButton">Gestion du blog</button></a>
        </div>
    </div>
    </div>
    {% include "partials/footer.html.twig" %}
{% endblock %}
