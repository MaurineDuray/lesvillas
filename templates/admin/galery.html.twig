{% extends 'base.html.twig' %}

{% block title %}Villas Blue{% endblock %}

{% block body %}
    <div class="slide" id="introGalery">
         <img id="intro-logo" src="{{asset('images/logo-03.png')}}" alt="Logo">
        <div class="sep">
            <img src="{{asset('images/separation-bottom.png')}}" alt="separation">
        </div>
    </div>
    <div class="slide" id="dasboardTitle">
        <h3 class="script"> Boujour {{app.user.firstName}},</h3>
        <h2>Bienvenue dans le tableau de bord !</h2>
        <a href="{{path('admin')}}">Retour au tableau de bord</a>
        
        
    </div>
   <div class="tableau">
   <h1 style="text-align: center">Gestion de la galerie de "{{immo.Titre}}"</h1>
   
    <div class="message" style="display: flex; justify-content:center; flex-wrap: wrap">
    <div class="admingalery" style="display:flex; flex-wrap: wrap">
    {% for picture in pictures %}
       <div class="card-img">
            <a class="deleteBtn" href="{{path('admin_galery_delete',{'id': picture.id})}}">X</a>
            <img style="width: 300px" src="../../upload/{{picture.file}}" alt="image du logement {{picture.immoId.Titre}}">
        </div>
    {% endfor %}
    </div>
   
    {{form_start(myform)}}
        {{form_widget(myform)}}
    {{form_end(myform)}}
    </div> 
    </div>
    </div>
    {% include "partials/footer.html.twig" %}
{% endblock %}
