{% extends "base.html.twig" %}

{% block title %}Modification du mot de passe de l'utilisateur{% endblock %}

{% block body %}
    <div class="slide" id="introGalery">
         <img id="intro-logo" src="{{asset('images/logo-03.png')}}" alt="Logo">
        <div class="sep">
            <img src="{{asset('images/separation-bottom.png')}}" alt="separation">
        </div>
    </div>
    <div class="slide" id="formulaire">
        <h1 style="text-align: center">Modification du mot de passe de l'utilisateur</h1>
        {{form_start(myform)}}

            {{form_widget(myform)}}
            <button type="submit" class="btn btn-warning">Enregistrer la modification du mot passe</button>
        {{form_end(myform)}}
    </div>
    {% include "partials/footer.html.twig" %}
{% endblock %}